// GENERATED by gen-docs.mjs — do not edit by hand.
export interface Doc { slug: string; group: string; file: string; title: string; desc: string; md: string }
export const DOCS: Doc[] = [{"slug":"why-assay-is-different","group":"Protocol","file":"WHY-ASSAY-IS-DIFFERENT.md","title":"Why Assay is different","desc":"How our lending differs from Aave/Kamino/Suilend — and what is actually enforced today.","md":"# Why Assay is different — and why it's safer\n\nA plain-language explainer of how Assay's lending protocol differs from the lending apps that\nalready exist on Sui and Solana, and why a user is safer here.\n\n---\n\n## What everyone else does (the status quo)\n\nThe established lending protocols — **Aave**, **Kamino**, **Solend/Save**, **MarginFi** on Solana;\n**Suilend**, **NAVI**, **Scallop** on Sui — are all variations of the same design:\n\n- You deposit collateral, you borrow another asset against it.\n- A **smart contract** enforces the rules (how much you can borrow, when you get liquidated).\n- Prices come from an **oracle** (Pyth, Switchboard, Chainlink).\n- If your loan gets unhealthy, **liquidators** repay it and take your collateral.\n\nThe thing they all have in common: **you're trusting audited code.** Smart people (audit firms)\nread the contract and looked for bugs. That's the whole safety model — \"experts checked it, and\nit's been running a while without blowing up.\" It usually works. But the multi-billion-dollar\nhacks in DeFi are almost all the same story: audited code that had a bug nobody caught.\n\n---\n\n## What Assay does differently\n\n> **Read this first — the design vs. what is deployed.** Points 1 and 2 below describe where this\n> is going, and each one states its real status inline. Points 3, 4, and 5 are live and enforced on\n> every loan today. The full status table is in the [README](../README.md); everything known-open is\n> in [OUTSTANDING.md](OUTSTANDING.md).\n\n**1. The core rule is designed to be mathematically *proven*, not just audited.**\nAssay's enforcement is built around **dregg**, a formally-verified (Lean 4) kernel. The one\ninvariant that keeps a lending protocol solvent —\n\n> *you can never borrow more than your collateral safely supports*\n> (`debt ≤ collateral × conservative_price × LTV`)\n\n— is **machine-checked math** in the kernel. Not \"we tested it,\" not \"an auditor read it\" — a proof\nassistant verified it holds for *every* possible input.\n\n*Status: the kernel is real and verified, but it is not yet what gates the live deployment.* When\nthe kernel is absent the operator falls back to an equivalent in-process LTV check and reports which\none ran via `authMode`. On Robinhood Chain there is no dregg at all — the limit is enforced by the\nSolidity contract against a Chainlink feed. That is the same guarantee Aave gives, and we describe\nit that way rather than borrowing the kernel's credibility for it.\n\n**2. Settlement is designed to be gated by a real cryptographic proof, on-chain.**\ndregg produces a zero-knowledge proof (STARK → Groth16) that a batch of loans is valid, and that\nproof is **verified on-chain by Sui's native `sui::groth16`** before the batch finalizes.\n\n*Status: the verifier is deployed and live on Sui; the circuits are not.* `dregg_lending` cannot\noriginate and `settle_batch` cannot succeed until both circuits are re-proven upstream, and the\ncircuits **have never been audited** — `circuit/` currently holds a Poseidon gadget and no\nconstraint system. The gate is therefore **disabled rather than quietly trusted**, which is the\nonly honest way to ship a proof system that isn't finished.\n\n**3. The oracle is conservative and fails *closed*.**\nMost protocols price you at the oracle's mid price. Assay prices collateral at the **conservative\nprice** (`price − 2× the oracle's own confidence band`) — it deliberately under-values your\ncollateral so a noisy print can't over-lend. A **stale** price is refused outright rather than\nrisked. Refusing is the safe default; most protocols keep lending.\n\n*Status, precisely:* on Sui, a closed underlying market **tightens** the staleness bound (60s → 15s)\nrather than refusing the loan — `operator/pyth.mjs` does not fail closed off-session. Outright\nsession refusal exists only in `AssayMarkets.canBorrow` on the Robinhood Chain port, which is not\nyet deployed. Closing that gap on Sui is tracked in [OUTSTANDING.md](OUTSTANDING.md).\n\n**4. Non-custodial by construction.**\nThe operator that authorizes a loan **never holds your funds and never co-signs your transaction**\n(`disburse_attested` takes no `OperatorCap` — there is no two-party transaction).\nIt signs a one-time cryptographic *attestation* over your exact loan terms; your own wallet sends\nthe transaction, and the on-chain contract verifies the attestation. Nobody can move your\ncollateral except the on-chain contract rules.\n\n**5. Real-world assets and crypto in one venue, with honest risk tiers.**\nTokenized stocks and crypto in the same pool, but each gets risk discipline appropriate to it:\ncrypto is 24/7 so it gets higher LTV; single-name stocks get **lower** LTV plus the market-hours\noracle guard, because a tokenized share trades 24/7 while the real stock market is only open\n~6.5 hours a day — that gap is a risk, and we price it in. On Robinhood Chain the contract enforces a\n**minimum 20-point gap** between LTV and the liquidation threshold — that gap is the invariant,\nchosen to absorb a weekend the position cannot be liquidated into. The launch proposal is 35/55,\nwhich is a governed parameter, not a protocol constant. Fork-tested against the real Chainlink\nfeed at 35/55, a position at max LTV survives a 30% drop and is underwater by 40%; the arithmetic\nbreak point is −36.4% (`0.35 / 0.55`).\n\n**6. Multi-chain, because the collateral is.**\nThe assets worth borrowing against do not all live on one chain, so Assay deploys to each on that\nchain's own terms rather than bridging everything to a preferred home:\n\n- **Sui** — where the verified-kernel path is built. Move's type system hashes the collateral's\n  type into the loan commitment, so a position cannot be reopened against a cheaper asset; that is\n  enforced by the type system rather than by a check we remembered to write.\n- **Robinhood Chain** (Arbitrum Orbit L2, chainId 4663) — where the real tokenized equities already\n  are, so an agent can buy a stock and borrow against it without a bridge. We could not locate a Chainlink\n  sequencer-uptime feed for this chain — their docs say one exists, we could not find it, and their\n  docs have already been wrong about this chain once — so rather than assume, we run a keeper\n  heartbeat (`LivenessOracle`) and keep liquidations disabled through a grace period after any\n  outage. Note it gates liquidation only; borrowing is not blocked by an undetected outage.\n\nDifferent chains give different guarantees. We state which one you are getting per deployment\ninstead of averaging them into a single marketing claim.\n\n---\n\n## Why *you're* safer here (in one paragraph)\n\nEvery other lending app is \"trust the audit.\" Assay is built to be \"trust the proof\" — and until\nthat lands, we tell you exactly which of the two you are getting. The single rule that can\nbankrupt a lending protocol — letting someone borrow more than their collateral covers — is\n**designed to be enforced by a formally-verified kernel** on Assay — see the status table in the\nREADME for what is live today. What you get right now is a conservative valuation, an on-chain limit\nenforced by the contract that holds the money, and a system that **refuses** rather than guesses\nwhen a price is stale or a market is shut. What you get when the kernel path lands is that same\nrule, machine-checked. We publish which one is running.\n\n---\n\n## Being honest about the limits (what proof does *not* cover)\n\nFormal verification is powerful but it is not magic, and overclaiming would be the opposite of safe:\n\n- **It covers the enforcement invariant, not every line of the app.** The proven part is the\n  solvency rule + the on-chain proof check. The website, the operator service, and the liquidation\n  keeper are ordinary software with ordinary risk.\n- **Oracle risk still exists.** We make it conservative and fail-closed, but if Pyth itself\n  published a badly wrong price within its confidence band, that's a residual risk (as it is\n  everywhere).\n- **Liquidation is not instant.** Like all lending, a fast crash can outrun liquidators; the\n  conservative LTV is the buffer.\n- **The current hosted demo runs a fallback.** The live devnet demo authorizes loans with an\n  operator-side LTV+oracle check (the serverless host has no Rust runtime for the dregg kernel);\n  the **on-chain guards are fully real**, and the **verified kernel** runs in the full (non-serverless)\n  deployment. The response is labeled `authMode` so it's never misrepresented.\n- **It's early.** Devnet, test assets, tiny amounts. None of this has the battle-testing that the\n  incumbents have — proof reduces a *class* of risk; time reduces a different class.\n\nThe honest pitch: **Assay is built to remove the single largest category of DeFi lending failure —\na bug that lets the protocol lend more than it should — by proving that path instead of hoping the\naudit caught everything.** The kernel that does it is real and verified; wiring it to be the thing\nthat gates every live loan is the work still in front of us, and the status tables say exactly how\nfar along that is.\n\nIn the meantime we do the unglamorous version of the same idea: **we attack our own code and publish\nwhat we find, clean or not.** Six adversarial rounds on the Move protocol produced 66 confirmed\nfindings and not one round came back clean the first time; the Solidity port's first round found 19\nconfirmed and refuted 5. All of it is in [audits/](audits/), including the rounds that made us look\nbad, because an audit trail you only publish when it is flattering is not an audit trail.\n"},{"slug":"architecture","group":"Protocol","file":"ARCHITECTURE.md","title":"Architecture","desc":"The four tiers: frontend, operator, on-chain program, and oracle.","md":"# Assay — system architecture (V1 MVP)\n\nAssay is an RWA lending market **designed around a formally-verified risk kernel** (see the README for what that enforces today): borrow USDC against tokenized stocks\n(xStocks), where every borrow is authorized in-kernel by dregg and (eventually) settled\nagainst a zk proof. This doc maps the tiers, who signs what, and what's built vs. to-build.\n\n## The four tiers\n\n```\n┌─────────────────────────────────────────────────────────────────────┐\n│ 1. FRONTEND  (Assay web app — the thing users see)                    │\n│    wallet connect · markets · borrow/repay · supply/withdraw · positions │\n└───────────────┬───────────────────────────────┬───────────────────────┘\n                │ direct-signed txs             │ borrow / liquidate\n                │ (deposit/withdraw/repay)      │ (needs dregg authorization)\n                ▼                               ▼\n┌───────────────────────────┐      ┌────────────────────────────────────┐\n│ 3. SOLANA PROGRAM          │◀─────│ 2. OPERATOR API  (Node service)     │\n│    dregg_lending_async     │ co-  │    runs dregg_borrow (kernel LTV/   │\n│    8 instructions          │ sign │    oracle authorize) + co-signs      │\n│    (deposit/borrow/…)      │      │    disburse; runs the keeper         │\n└───────────┬────────────────┘      └──────────────┬─────────────────────┘\n            │ reads price                          │ fetches price\n            ▼                                       ▼\n┌───────────────────────────┐      ┌────────────────────────────────────┐\n│ (on-chain state)           │      │ 4. PYTH ORACLE (Hermes REST)        │\n│  Config · Pool · Positions │      │    live TSLAx/NVDAx/… equity prices │\n└───────────────────────────┘      └────────────────────────────────────┘\n```\n\n## Who signs what (the key design fact)\n\n| Action | Signers | Path | Why |\n|--------|---------|------|-----|\n| **Deposit** (supply USDC) | lender | **direct from wallet** | no risk decision — just adds cash |\n| **Withdraw** | lender | **direct from wallet** | proportional claim, program-checked |\n| **Repay** | borrower | **direct from wallet** | pays debt, program-checked |\n| **Borrow** (disburse) | **operator + borrower** | **via Operator API** | dregg must authorize LTV/oracle BEFORE money moves |\n| **Liquidate** | operator + liquidator | keeper (Operator API) | dregg authorizes \"underwater\"; not user-facing |\n| **Settle batch** | anyone (permissionless) | operator/cron | proof-gated batch finalize |\n\n**The one thing that forces a backend:** a borrow can't be pure client-side — the dregg\nkernel authorization (`dregg_borrow`, a Rust program) runs off-chain and the **operator\nco-signs** the disburse. Everything else the user's wallet signs directly. So the V1 MVP is\nfrontend + a thin operator API, not a pure dapp.\n\n## What's built today (backend, all tested on localnet)\n\n- **Solana program `programs/dregg_lending_async`** — 8 instructions:\n  `0 init_config · 1 disburse · 2 settle_batch · 3 init_pool · 4 deposit · 5 withdraw · 6 repay · 7 liquidate`.\n  Token-2022 collateral leg (real xStocks), borrow-index interest, exposure cap, proof-gated settle.\n- **Oracle** — `operator/pyth.mjs`: Hermes fetch + fail-closed policy (conservative price,\n  staleness tighter off-hours, confidence ceiling, circuit breaker). `operator/assets.mjs`:\n  the TSLAx registry (real mint + real Pyth feed).\n- **Operator logic** — `operator/operator-service.mjs` (dregg authorize → disburse),\n  `operator/keeper.mjs` (liquidation). These are scripts today, not yet an HTTP API.\n- **Instruction builders** — currently inline in the localnet test `.mjs` files\n  (`tests/localnet/*.mjs`) + `mainnet-demo/borrow-demo.mjs`. To be extracted into a shared TS SDK.\n- **Sui twin** — `move/dregg_lending_async` (same model, `sui move test` green).\n\n## What's NOT built (the V1 MVP gap)\n\n- **The web app** — no frontend exists; Assay is a static HTML mockup (Artifact).\n- **The Operator API** — the borrow/keeper logic is scripts, not an HTTP service.\n- **A TS client SDK** — instruction builders are copy-pasted in test files, not a reusable lib.\n- **A positions/dashboard view** — not in the current design; needed so a borrower can see + manage loans.\n- **Devnet deployment + a funded pool** — flows run on localnet; need a persistent devnet deploy.\n\nSee `V1-MVP-PLAN.md` for the phased build and `COMPONENT-MAP.md` for every UI element's wiring.\n"},{"slug":"scope-robinhood-chain","group":"Protocol","file":"SCOPE-robinhood-chain.md","title":"Robinhood Chain scope","desc":"The Solidity port: real Stock Tokens, Chainlink feeds, and what the chain does and doesn't give us.","md":"# Scope — Assay on Robinhood Chain\n\nTarget MVP: a user signs in via their own MCP, an AI agent buys stock on Robinhood, the Stock\nToken lands in their self-custody wallet, and they borrow against it in Assay.\n\nAll contract facts below were read from the **deployed, verified source** on\n`robinhoodchain.blockscout.com`, not from documentation — the docs contradicted themselves and\nseveral widely-cited third-party write-ups are wrong about transfer restrictions.\n\n---\n\n## 0. The finding that reframes the port\n\n`AAPL • Robinhood Token` (`0xaF3D76f1834A1d425780943C99Ea8A608f8a93f9`) is an eip1967 beacon proxy\nto a verified `Stock` implementation (`0xb35490d6f9163DE4F80d88dc75c3516eb64C5aE2`):\n\n```solidity\ncontract Stock is IStock, AccessControlled, OraclePausable, ERC20ScaledUIUpgradeable\n\nfunction transfer(address to, uint256 value) public override\n    onlyNotPaused onlyNotBlocked(to) onlyNotBlocked(_msgSender())\n\nmodifier onlyNotBlocked(address account) {\n    if (IAccessControlsRegistry(ACCESS_CONTROLLED_REGISTRY).isBlocked(account)) revert Blocked(account);\n    _;\n}\n```\n\n`isBlocked` is a **deny-list, not an allow-list**. Any address — including an arbitrary\nthird-party lending pool — can receive and hold Stock Tokens today. No KYC gate, no whitelist, no\nintegration agreement at the contract layer.\n\n**And Robinhood Chain has on-chain Chainlink price feeds for every Stock Token**, using the same\n`AggregatorV3Interface` / `latestRoundData()` as any crypto feed.\n\nThat second fact is the important one. On Sui there is no on-chain oracle, so the operator signs\nprices off-chain and the contract verifies an ed25519 attestation. Essentially every mechanism\nhardened across audit rounds F2–R6 — expiry windows, nullifiers, pool binding, domain separation,\nthe two-key liquidation dance, `MAX_ATTEST_WINDOW_S`, the whole cross-language byte-pinning\napparatus — exists **to compensate for the absence of an on-chain price**.\n\n**The port is therefore a simplification, not a translation.** Roughly 60% of the Sui contract's\ncomplexity does not need to exist on Robinhood Chain.\n\n---\n\n## 1. What carries over, what disappears\n\n### Carries over — the invariants the audits actually bought\n\nThese are design conclusions, not code. They transfer directly to Solidity:\n\n| Invariant | Origin |\n|---|---|\n| Authorisation must bind the **loan's actual terms** — debt, collateral amount **and type** | F1, R5-1 |\n| Liquidation needs a **real health check** and must **refund surplus** to the borrower | F3 |\n| Every capability must be **bound to the pool it governs** | R2-1 |\n| Position objects must be **bound to their pool** | R3 |\n| Exposure ledgers must **release on close**, exactly once, with no double-release path | R5-3, R6-B1 |\n| Rate parameters must be **bounded**; overflow must saturate, never trap funds | R2-2 |\n| Privilege changes need a **timelock**, so rotate-and-use is not atomic | R5-2 |\n| Guards need **mutation tests** — four guards in this codebase were deletable with a green suite | R4, R6 |\n\n### Disappears — machinery that only existed for the missing oracle\n\n- The entire ed25519 attestation path (`attest_msg`, `disburse_attested`, expiry, window, nullifier)\n- Domain separation between message types (only needed because two things were signed)\n- Cross-language byte-pinning between TS and Move\n- The operator's role as *price authority*\n- Two-party liquidation, the rotation timelock, `pending_pubkey` state\n- `pause` as a key-compromise kill switch\n\nLiquidation becomes **permissionless with an on-chain health check** — which is what F3 was\nreaching for and could not achieve without an oracle.\n\n### The ZK/dregg layer\n\n`settle_batch`, the Poseidon accumulator, and `loan_commit_of` were the \"provably safe\"\ndifferentiator. **This is the one real decision to make.** With on-chain LTV enforcement, the\nproof is no longer load-bearing for solvency — the contract can enforce the invariant directly.\nOptions:\n\n1. **Drop it for v1 on this chain.** Simplest, honest, ships fastest. The safety story becomes\n   \"the contract enforces LTV against a Chainlink feed\" — the same as Aave, which is a weaker\n   claim than \"provably safe\" but is *true today*, unlike the current claim.\n2. **Keep it as an additional attestation** over the dregg kernel's decision, layered on top of\n   on-chain enforcement. Preserves the differentiator; costs the circuit work that is already\n   blocking `dregg_lending` origination.\n\nRecommendation: **ship v1 without it, keep the hook.** The circuit work is upstream-blocked and\nshould not gate a working product.\n\n---\n\n## 2. What is NEW and Robinhood-specific\n\nThese have no analogue in the Sui design and are the real engineering risk.\n\n### 2.1 `adminBurn` — collateral can be destroyed in place\n\n```solidity\nfunction adminBurn(address from, uint256 amount) public override onlyRole(ADMIN_BURNER_ROLE) {\n    _burn(from, amount);\n}\n```\n\nNote what is **absent**: no `onlyNotPaused`, no `onlyNotBlocked`. Robinhood can burn Stock Tokens\nout of **any** address, including a live lending pool, unconditionally.\n\nAn open loan can become instantly unsecured with **nothing left to liquidate**.\n\nDesign consequences:\n- **Never trust a stored collateral figure.** Read `balanceOf(pool)` and reconcile against the sum\n  of recorded positions on every state-changing call.\n- Add an explicit **shortfall path**: when actual < recorded, the deficit is socialised to lenders\n  or absorbed by reserves — decided in code, not discovered in production.\n- This risk must be **disclosed in the UI and priced into LTV**. It is not a bug to fix; it is a\n  property of the collateral.\n\n### 2.2 `uiMultiplier` — corporate actions rescale the position mid-loan\n\n```solidity\nfunction balanceOfUI(address a) public view returns (uint256) {\n    return Math.mulDiv(balanceOf(a), uiMultiplier(), DENOMINATOR);\n}\nfunction _updateUIMultiplier(uint256 newMultiplier, uint256 effectiveAt_) internal { ... }\n```\n\n`balanceOf()` is the raw amount and is **stable**; `balanceOfUI()` is the share-equivalent and\n**changes on splits and other corporate actions**. The multiplier can be **scheduled** with a\nfuture `effectiveAt`, so it changes **without any transaction touching the pool**.\n\n- Collateral must be valued as **`balanceOfUI × price`**, never `balanceOf × price`. Pricing the\n  raw balance misprices every position by the split ratio the moment a split lands — wrongly\n  liquidating healthy loans or silently under-collateralising others.\n- Health checks must re-read `uiMultiplier()` every time; caching it is a bug with a delayed fuse.\n- Add a **scheduled-multiplier alert** so a pending corporate action is visible before it fires.\n\n### 2.3 Global pause traps repay and liquidate\n\n`paused()` returns token-level **OR** registry-level pause. When Robinhood pauses, `transfer` and\n`transferFrom` revert — so **borrowers cannot repay and liquidators cannot seize**, while interest\nkeeps accruing.\n\n- Accrual must be **suspended while the collateral token is paused**, or borrowers are charged for\n  a window in which they were structurally unable to repay.\n- Liquidation eligibility should be evaluated on **unpaused time**, not wall-clock.\n\n### 2.4 Beacon proxy — the token is upgradeable\n\nAll Stock Tokens share one implementation behind a beacon. Transfer semantics, the multiplier\nmechanism, and the burn powers can all change for every token simultaneously. Assume the interface\nis stable; assume the **policy is not**.\n\n### 2.5 Collateral is a Jersey debt token, not equity\n\nStock Tokens are **tokenised debt securities** issued by Robinhood Assets (Jersey) Limited, giving\neconomic exposure with **no legal or beneficial right in the underlying share**. Collateral quality\nis Robinhood-Jersey counterparty risk. This belongs in risk parameters and in plain language in the\nUI — especially alongside any safety claim.\n\n---\n\n## 3. Contract surface (v1)\n\nSolidity, Robinhood Chain (Arbitrum Orbit, EVM). Borrow asset: **USDG**\n(`0x5fc5360D0400a0Fd4f2af552ADD042D716F1d168`).\n\n```\nAssayPool.sol          ERC-4626-style lender vault, USDG deposits, share accounting\n                       borrow-index accrual (port the Sui curve, keep the R2-2 bounds\n                       and saturating math — that logic was correct and is chain-agnostic)\n\nAssayMarkets.sol       per-collateral registry: Stock Token address, Chainlink feed,\n                       ltvBps, liqThresholdBps, liqBonusBps, per-collateral cap\n                       (the isolation cap from the Sui design, which held up in audit)\n\nAssayBorrow.sol        openPosition: pull collateral, read latestRoundData + uiMultiplier,\n                       enforce LTV on-chain, disburse USDG\n                       repay: >= owed with change returned (fixes known-open F5 by\n                       construction — do NOT port the exact-equality bug)\n\nAssayLiquidate.sol     permissionless. health = balanceOfUI x price x liqThreshold / debt\n                       seize only debt x (1 + bonus), REFUND SURPLUS (F3)\n                       reconcile balanceOf before seizing (adminBurn shortfall path)\n\nAssayGuard.sol         oracle staleness + sequencer-uptime checks, pause-aware accrual,\n                       shortfall accounting, timelocked admin (R5-2)\n```\n\n**Reuse rather than re-derive:** OpenZeppelin for ERC-20/4626/AccessControl/TimelockController,\nand Chainlink's `AggregatorV3Interface`. The audit findings that were about *hand-rolled*\nprimitives (low-order pubkey validation, BCS byte pinning, limb splitting) vanish entirely —\nthat whole class of bug came from writing crypto by hand.\n\n**Oracle discipline to port from `operator/pyth.mjs`** — that layer was repeatedly confirmed sound:\nstaleness bounds, confidence-interval rejection, fail-closed on error, market-hours awareness. On\nan L2 also check **sequencer uptime** before trusting a feed; this is the standard Arbitrum\nL2 gotcha and is not in the Sui design because it could not be.\n\n---\n\n## 4. The agent / MCP flow\n\nThe half that is **available today** and is the actual product hook:\n\n```\n1. User connects their own MCP client to Robinhood's Trading MCP\n   -> \"connect a third-party AI agent to a dedicated Robinhood account\"\n   -> agent places the equity order under the user's own credentials.\n      Assay never holds the account. No custody, no licensing exposure.\n\n2. Robinhood settles the position as a Stock Token in the user's\n   self-custody Robinhood Wallet on Robinhood Chain.\n\n3. Assay dApp (or an Assay MCP tool) reads the wallet, prices via Chainlink,\n   quotes a borrow.\n\n4. User approves + openPosition in one tx. USDG lands in their wallet.\n   Collateral sits in AssayPool, on the same chain, no bridge.\n```\n\nAssay is **never** a custodian at any step, which preserves the non-custodial property that the\ncustody alternative would have destroyed.\n\n**Worth building: an Assay MCP server.** If the user already drives Robinhood through MCP, exposing\n`assay.quote`, `assay.borrow`, `assay.health`, `assay.repay` as MCP tools makes the whole flow one\nconversation. That is a genuine differentiator and is small next to the contract work.\n\n**Verify before relying on it:** Trading MCP availability by jurisdiction, whether agent trading\ncovers the equities that become Stock Tokens, and the settlement path/latency from order fill to\ntoken in wallet.\n\n---\n\n## 5. Phasing\n\n| Phase | Work | Gate |\n|---|---|---|\n| **0 — Spike** ✅ **DONE** | All assumptions verified against live mainnet with zero gas and no keys: deny-list default-open, sequencer uptime feed exists, testnet exists, and 67 contracts already hold Stock Tokens in production. `rh-chain/phase0-verify.mjs`, 7/7. | ✅ passed |\n| **1 — Core** (2–3 wks) | AssayPool + Markets + Borrow + Liquidate, on-chain LTV, surplus refund, `balanceOfUI` pricing, sequencer check | Full test suite incl. **mutation tests on every guard** |\n| **2 — RH hazards** (1 wk) | `adminBurn` reconciliation + shortfall path, pause-aware accrual, scheduled-multiplier handling | Fork tests against real Stock Tokens |\n| **3 — Agent** (1–2 wks) | Assay MCP server, dApp borrow flow, Robinhood Wallet integration | End-to-end on testnet |\n| **4 — Audit** | Fresh adversarial rounds on the Solidity | All-clean round before mainnet |\n| **5 — Hybrid** (optional) | Sui pools for non-Robinhood RWA; shared markets/risk config. **CCIP** already moves Stock Tokens cross-chain — evaluate, do not assume Sui is a supported lane. | — |\n\n---\n\n## 6. Open questions\n\n**RESOLVED in Phase 0 (2026-07-20)** — verified against live mainnet, chainId 4663:\n\n- ✅ **Deny-list confirmed empirically.** `isBlocked()` returns false for a never-used address, the\n  zero address, vitalik.eth and a plain mainnet contract address. Default-open. Registry is\n  `0xe10b6f6b275de231345c20d14ab812db62151b00`. Run `node rh-chain/phase0-verify.mjs` to re-check.\n- ✅ **Sequencer uptime feed EXISTS.** Chainlink publishes an L2 Sequencer Uptime Feed for\n  Robinhood Chain: *\"check it before reading any price.\"* Liquidation safety is achievable.\n- ✅ **There is a TESTNET** (chainId 46630, `https://rpc.testnet.chain.robinhood.com/rpc`), so\n  Phases 0–3 need no real money.\n- ✅ Registry and all four sampled Stock Tokens are currently unpaused; `uiMultiplier` is 1.0\n  across AAPL/TSLA/NVDA/SPY (no corporate action applied yet — so a split has never yet exercised\n  that code path in production).\n- ⚠️ **Stock feeds update 24/5, following market hours — they go STALE nights and weekends.**\n  This is the significant one and it changes risk design. Robinhood Chain markets Stock Tokens as\n  24/7 tradeable, but the *price* is not 24/7. Borrowing against a Friday-close price through a\n  weekend gap is the classic RWA blowup: the stock gaps on Monday open and no liquidation was\n  possible in between. Required response, decided deliberately rather than by default:\n  a materially lower LTV while the feed is stale, and/or blocking new borrows off-hours, and\n  liquidation eligibility evaluated on unpaused, fresh-feed time. The Sui operator already\n  encoded this instinct (`maxStaleOffHoursSecs`, market-hours awareness in `operator/pyth.mjs`) —\n  on this chain it becomes an on-chain concern.\n\n**Still open:**\n\n1. ~~Feed heartbeats and deviation thresholds~~ **RESOLVED.** All 34 Robinhood equity feeds (and\n   the crypto feeds) are **86400s heartbeat / 0.5% deviation**, from Chainlink's feed directory.\n   This corrected a real bug: the first `StaleFeedGuard` draft used bounds TIGHTER than the\n   heartbeat and would have rejected healthy prices constantly — the live AAPL feed read $326.49\n   at ~2.4h old, which my own guard would have refused. Regenerate with\n   `node rh-chain/script/fetch-feeds.mjs`, which exits non-zero if the heartbeat ever changes.\n\n2. 🚩 **BLOCKER — the L2 sequencer uptime feed cannot be found.** Robinhood's docs say\n   *\"Chainlink provides an L2 Sequencer Uptime Feed for this; check it before reading any price.\"*\n   It is not on Chainlink's canonical L2 sequencer feed list, not in the Robinhood feed directory,\n   not findable by name search, and every contract from Chainlink's deployer on this chain\n   resolves to a price feed. Their docs have already been wrong once about this chain, so the\n   claim alone is not evidence.\n\n   Without it: during an outage nothing executes and nothing can be liquidated; on resumption a\n   backlog runs against prices users had no chance to react to. The 24h heartbeat means staleness\n   detection would not notice an outage for a full day — far too slow to substitute.\n\n   `StaleFeedGuard` accepts `address(0)` and exposes `sequencerCheckDisabled()` so the gap is\n   advertised rather than silently skipped. **Resolve before mainnet**: either locate the feed\n   (ask `chain-developers-group@robinhood.com`) or accept it explicitly with the LTV buffer and an\n   off-chain pause keeper as the compensating controls.\n2. **Has `adminBurn` ever been used?** Check historical `Transfer`-to-zero events from admin roles.\n   Frequency changes whether this is theoretical or operational.\n3. ~~Who holds `ADMIN_BURNER_ROLE`?~~ **RESOLVED — and it is the worst of the plausible answers.**\n   Recovered from `RoleGranted` logs on the registry (the contract lacks `AccessControlEnumerable`,\n   so RPC enumeration reverts):\n\n   | Role | Holder | Type |\n   |---|---|---|\n   | `ADMIN_BURNER_ROLE` | `0x957b6de6525c63349f7619743ef1e0ad93cd74d4` | **EOA** |\n   | `TOKEN_PAUSER_ROLE` | `0xfccf56b674113d9c4eb0f9b3370930ced9e6ab23` | **EOA** |\n   | `MINTER_ROLE` | `0x2b94105fff37630f98e1f24811dad588fc5c3a87` | **EOA** |\n   | `MULTIPLIER_UPDATER_ROLE` | `0x92905e8d0e2301ba143215b8d86d63ffd4188143` | **EOA** |\n   | `DEFAULT_ADMIN_ROLE` | `0xd6f8378f8e440c65f8382f5f2728c78dfd55b66d`, `0x074377a78a9710a1d47244f89797718b4f491279` | **EOA** |\n\n   Every privileged role is a plain externally-owned account. No multisig, no timelock, no on-chain\n   governance delay anywhere in the stack. A single key can burn collateral out of a live pool; a\n   single key can freeze every Stock Token; `DEFAULT_ADMIN_ROLE` can grant itself any other role.\n\n   **Honest caveat:** an EOA can still be a hardware-backed or MPC-custodied key with strong\n   operational controls. Chain data can only tell us it is *not a contract* — it cannot show us\n   Robinhood's internal controls. The correct statement is therefore: **there is no on-chain\n   mitigation; the protection is entirely institutional trust in Robinhood.**\n\n   The deny-list is also **actively used** — 246 `Blocked` events on the registry (sampled entries\n   are EOAs, including burn addresses like `0x…dead`), against 4 `Unblocked`. It is live\n   operational machinery, not a dormant capability, so a pool address being blocked is a real\n   scenario to design for rather than a theoretical one.\n4. **Trading MCP jurisdiction coverage** vs Stock Token availability (120+ countries, varies).\n5. ~~Can a CONTRACT receive a Stock Token?~~ **RESOLVED — production evidence, no deployment\n   needed.** 67 contract holders across the four sampled Stock Tokens, including a Uniswap-style\n   `PoolManager` holding 4,806 NVDA, a **`StockVault`** holding 18.4 AAPL, and third-party\n   `Index Basket APPLCAT` / `Index Basket 401k` contracts. Contracts do not merely *tolerate*\n   Stock Tokens — they already custody them at scale, and some of those are clearly third-party\n   rather than Robinhood infrastructure. **Phase 0 is complete.**\n\n---\n\n## 7. What this does to the existing Sui work\n\nNot wasted, but be honest about what transfers:\n\n- **The Move implementation does not port.** ~2,000 lines stay on Sui.\n- **The design does.** Section 1's invariant table is the actual output of six audit rounds, and\n  every item on it was learned by finding a real hole.\n- **The audit method ports completely** — adversarial rounds with independent verification, and\n  mutation-testing every guard. That method caught four guards in this repo that were deletable\n  with a fully green suite, and three false claims in commit messages. Use it from day one on the\n  Solidity rather than bolting it on at the end.\n- **The Sui deployment remains valid** for non-Robinhood RWA collateral, if the hybrid is pursued.\n"},{"slug":"ltv-risk-framework","group":"Risk","file":"LTV-RISK-FRAMEWORK.md","title":"LTV & risk framework","desc":"How loan-to-value limits are chosen per asset from a stress model.","md":"# LTV & risk-parameter framework\n\nHow Assay sets loan-to-value (LTV) and liquidation thresholds safely — the methodology,\nhow the big lenders do it, and the concrete schema for our assets (esp. the RWA gap-risk\nthat makes equities different from crypto).\n\n## The one question LTV answers\n> If the collateral price falls, can a liquidation still fully cover the debt before the\n> position goes underwater?\n\nLTV must leave enough headroom to survive **four things** between \"healthy\" and \"bad debt\":\n1. **Price drop during the liquidation window** — the time to detect + execute a liquidation.\n2. **Slippage** — the liquidator has to sell the collateral; thin liquidity = worse fills.\n3. **Liquidation penalty/bonus** — the incentive paid to liquidators (they buy collateral at a discount).\n4. **Oracle error/latency** — the price we act on may lag or be uncertain.\n\nSo there are **two** numbers per asset, and their gap is the safety:\n- **Max LTV** (borrow LTV) — the most you can borrow when opening. e.g. 40%.\n- **Liquidation Threshold (LT)** — the debt/value ratio that triggers liquidation. e.g. 50%.\n- **LT > Max LTV** always, so a fresh max loan isn't instantly liquidatable. And **LT itself is\n  set below the \"stress-survivable\" ratio** so a liquidation at LT still clears the debt.\n\n## How other platforms do it\n\n| Platform | Model | Who sets it | Key factors |\n|----------|-------|-------------|-------------|\n| **Aave** | Per-asset LTV / liq-threshold / liq-bonus; supply+borrow caps; isolation mode; e-mode for correlated pairs | Governance, advised by **risk firms (Gauntlet, Chaos Labs) running agent-based simulations** | volatility, on-chain liquidity/depth, oracle quality, correlation |\n| **Compound** | Collateral factor (= LTV) per asset | Governance | same |\n| **MakerDAO** | Min collateralization ratio per collateral type (inverse LTV), debt ceilings, stability fees, liquidation penalty | Governance + risk core units | volatility, liquidity, systemic risk |\n| **Morpho Blue** | **Immutable per-market LLTV** chosen at market creation from a whitelist; markets are **isolated** | Market creator; curators choose which to fund | oracle + LLTV picked for the pair |\n| **Kamino** | Per-asset LTV/LT + caps + e-mode | Governance/risk | volatility, liquidity, oracle |\n\n**The common thread:** LTV is a **governance risk parameter**, not a live computation. The\nnumber comes from a **stress/simulation model** of \"how bad can this get in a liquidation,\"\nand it's **monitored and adjusted** as volatility and liquidity change. The best shops\n(Aave via Gauntlet/Chaos) literally run Monte-Carlo simulations of price paths + liquidation\ncascades to pick the caps and thresholds. Smaller/newer protocols start with a\n**conservative rules table** and tighten/loosen from observation.\n\n## The risk inputs (what feeds the number)\n\n1. **Volatility** — the worst plausible drawdown over the liquidation window. Use a high\n   percentile (e.g. 99th) of historical + implied vol. Higher vol → lower LTV.\n2. **Liquidity / market depth** — how much can you liquidate without moving the price? Thin\n   liquidity → high slippage → **lower LTV AND a supply cap** (never let one market's\n   liquidation exceed what the market can absorb).\n3. **Oracle quality** — Pyth confidence width, update frequency, manipulation resistance.\n   Wide/slow oracle → more conservative. (We already value collateral at `price − k·conf`.)\n4. **Liquidation feasibility** — how fast can our keeper act? Slower → bigger buffer.\n5. **Gap risk (THE RWA-specific one — see below).**\n\n## The formula (how a threshold is derived)\nFor a stress drop `D` (a high-percentile adverse move over the liquidation window, **plus the\novernight gap for equities**), slippage `S`, and liquidation penalty `P`, the liquidation\nthreshold must satisfy: after the drop, the (discounted) collateral still covers the debt +\npenalty. Intuitively:\n\n```\nLT  ≈  (1 − D − S) / (1 + P)          # threshold survives a stress drop + slippage + penalty\nmaxLTV  =  LT − margin                # a fresh loan can't be instantly liquidatable (margin ~10%)\nsupplyCap  ≈  k · (market depth that can be liquidated in one window)\n```\n\nExample (a large-cap equity): plausible stress drop incl. an overnight gap `D≈35%`, slippage\n`S≈5%`, penalty `P≈8%` → `LT ≈ (1−.35−.05)/1.08 ≈ 0.55`. Round DOWN for safety → **LT 55%**,\n**maxLTV ~45%**. (Then tighten for single-name / volatile names.)\n\n## THE RWA DIFFERENCE — gap risk (our defining constraint)\nA tokenized stock (xStock) trades **24/7 on-chain, but the underlying share only trades\n9:30–16:00 ET**. Overnight / weekend / on a trading halt, the oracle price is a **stale\nlast-print** while the real value can gap on news (earnings, a halt, a macro shock). An\nattacker can borrow at Friday's price right before a Monday gap-down.\n\nThis is why **equities get much lower LTV than crypto even at similar volatility** — the LTV\nmust survive a *discontinuous* overnight move, not just intraday drift:\n- **Single-name stock** overnight gaps can be 10–30% on bad news → LTV **40%** (a 60% cushion).\n- **Index (SPYx)** is diversified → gaps far less → LTV **55%**.\n- **Crypto (BTC/ETH)** trades 24/7 — **no gap** (the token *is* the asset, always priced) → LTV **70%**.\n\nOur oracle policy already enforces the freshness side of this: **tighter max-staleness\noff-hours** (15s vs 60s) so a stale equity price is *refused*, and the `assetClass` branch\nkeeps crypto (always-fresh) from being wrongly refused. LTV handles the *magnitude* of the\ngap; the oracle handles *not lending on a stale print*.\n\n## Assay's schema (the per-asset table + how a new asset is parameterized)\n\n**To onboard a market, classify it and set params from this table (conservative to start):**\n\n| Class | Example | Max LTV | Liq threshold | Rationale |\n|-------|---------|--------:|-------------:|-----------|\n| Crypto major | cbBTC, ETH | 70% | 80% | 24/7, deep liquidity, robust oracle, no gap |\n| Crypto (volatile) | SOL | 60% | 72% | 24/7 but higher vol |\n| Index equity | SPYx | 55% | 65% | diversified → small gap |\n| Large-cap single equity | AAPLx, NVDAx | 40–45% | 55–58% | gap risk + single-name vol |\n| Volatile single equity | MSTRx, COINx | 30–35% | 45–48% | high vol + gap |\n| Yield-stable | USDY | 85% | 95% | near-peg, but accrues + issuer restrictions (not 100%) |\n| Tokenized treasury | OUSG | 90% | 96% | low vol — BUT permissioned transfers gate it (later) |\n\n**The onboarding checklist for a new asset's LTV:**\n1. **Classify** (crypto / index / single-equity / stable / treasury) → start from the row above.\n2. **Adjust for the specifics:** vol percentile, on-chain liquidity/depth (→ maybe lower + a\n   supply cap), oracle confidence width, and — for equities — the plausible **overnight gap**\n   for that name (single-stock > index).\n3. **Set LT from the stress formula**, then **maxLTV = LT − ~10% margin**.\n4. **Set a supply/borrow cap** sized to liquidatable depth (don't let a liquidation exceed\n   market depth in one window).\n5. **Add a reserve/insurance buffer** target (a borrow-fee cut) for residual gap-tail risk.\n6. **Review + monitor:** re-evaluate as vol/liquidity move; tighten fast, loosen slowly.\n\n## Who enforces it (the Assay-specific safety)\nThe LTV *value* is our risk decision (the table + process above). But unlike a normal\nprotocol that just trusts its off-chain risk team, **dregg enforces it provably**: the borrow\nturn is admitted only if `debt ≤ collateral · conservative_price · LTV` **in-kernel**, with\n`conservative_price = Pyth_price − k·confidence`, plus freshness + confidence caveats. So the\nrisk parameters are machine-checked at authorization, not asserted in a config. Liquidation\nuses the liquidation threshold the same way (`dregg_liquidate`).\n\n## v1 vs. later\n- **v1 (now):** the conservative rules table above + the stress-formula onboarding checklist +\n  the oracle discipline. Deliberately conservative LTVs (esp. equities) so we're safe while\n  small. TSLAx 40% / cbBTC 70% are already set this way.\n- **Later (scale):** commission a Gauntlet/Chaos-style **simulation** of price paths +\n  liquidation cascades to calibrate caps and thresholds precisely; add per-market isolation\n  (Morpho-style) so a bad market can't contaminate the shared pool; a funded reserve.\n"},{"slug":"interest-rate-model","group":"Risk","file":"INTEREST-RATE-MODEL.md","title":"Interest rate model","desc":"How supply/borrow rates are set by utilization to attract liquidity.","md":"# Interest rate model — how to price USDC deposits\n\n**The question:** what interest rate do we offer to get people to deposit USDC, so there's\nliquidity sitting in the pool for borrowers to draw? And how is that rate decided?\n\n**The short answer:** you don't pick a fixed rate. In lending protocols the rate is a **function\nof how much of the pool is being borrowed** (utilization). The market sets the equilibrium; you\nonly choose the *shape* of the curve. This is the same model Aave and Compound use, and it's the\nright one for Assay.\n\n---\n\n## 1. The mechanic: utilization drives the rate\n\nDefine **utilization** = how much of the supplied USDC is currently lent out:\n\n```\nU = totalBorrows / (idleCash + totalBorrows)\n```\n\n- **Borrowers** pay a *borrow APR* that **rises as U rises**.\n- **Suppliers** earn a *supply APY* = `borrowAPR × U × (1 − reserveFactor)`.\n  (Suppliers only earn on the portion actually borrowed — that's the `× U` — minus the protocol's cut.)\n\nWhy tie it to utilization? It's self-balancing:\n- **Low U** (lots of idle USDC) → low rates → cheap to borrow → attracts borrowing, and suppliers\n  earn little so they don't over-supply.\n- **High U** (pool nearly drained) → high rates → expensive to borrow (borrowers repay) and\n  lucrative to supply (new deposits arrive). This automatically pulls the pool back to a healthy level\n  and **always keeps a buffer so suppliers can withdraw.**\n\n## 2. The \"kinked\" curve (the standard shape)\n\nRates are flat-ish until a **target utilization** (the *kink*, `U*` ≈ 80–90%), then steep above it:\n\n```\nif U ≤ U*:   borrowAPR = base + slope1 · (U / U*)\nif U > U*:   borrowAPR = base + slope1 + slope2 · ((U − U*) / (1 − U*))\n```\n\n- **base** — rate at zero utilization (often 0–1%).\n- **slope1** — gentle rise up to the kink (this is the \"normal operating\" zone).\n- **U\\*** — the kink; the utilization you *want* the pool to sit near. Above it you're eating into the\n  withdrawal buffer, so…\n- **slope2** — a **steep** climb above the kink (e.g. +100–300% by U=100%) that makes running the pool\n  dry very expensive and yanks it back.\n- **reserveFactor** — the protocol's cut of borrow interest (e.g. 10–20%), funds treasury/insurance.\n\n## 3. How to actually pick the numbers (worked example)\n\nStart from the answer you want and work backwards. Suppliers won't deposit USDC unless the yield\nbeats their alternatives — roughly **~4–5% (T-bills / just holding USDC)** on the low end, and\n**~5–10%** in competing DeFi lending. So to attract liquidity, target a supply APY that's clearly\ncompetitive at the utilization you expect to run.\n\nSay you want **~10% supply APY at the 80% kink**, with a 10% reserve factor:\n\n```\nsupplyAPY = borrowAPR · U · (1 − reserveFactor)\n0.10      = borrowAPR · 0.80 · 0.90\nborrowAPR ≈ 13.9%   at the kink\n```\n\nSo you'd set: **base ≈ 0%, slope1 ≈ 14%** (reaches ~14% borrow APR at U*=80%), **slope2 ≈ 150–300%**\n(punishes >80%), **U\\* = 80%**, **reserveFactor = 10%**. Borrowers pay ~14% at target; suppliers\nearn ~10%; the protocol keeps ~1.5%.\n\nTune per environment:\n- **Bluechip/stable demand** (BTC/ETH collateral) → flatter curve, lower rates.\n- **Volatile/uncertain demand** → higher slope2 so scarcity self-corrects fast.\n- Higher target APY if you need to attract liquidity faster; lower once liquidity is deep.\n\n## 4. The real problem: bootstrapping (chicken-and-egg)\n\nThe IRM sets the *equilibrium*, but early on there's a cold-start problem: **no borrowers → low\nutilization → low supply APY → no reason to supply.** The dynamic rate alone won't bootstrap you.\nThe standard fixes:\n\n1. **Seed the pool yourself** (protocol-owned liquidity). You/treasury supply the initial USDC so\n   borrowers have something to draw from day one. (This is literally what `dev-up-sui.sh` does now —\n   it seeds the pool.)\n2. **Liquidity incentives** — emit a token / points to supplement organic supply APY early\n   (\"supply USDC, earn 10% interest **+** X in rewards\"). This is how essentially every new lending\n   protocol got its first liquidity. Sunset it as organic utilization grows.\n3. **Promotional fixed APY** early, then transition to the dynamic curve once there's real demand.\n4. **Reserve factor → insurance fund** — a slice of interest builds a backstop that makes suppliers\n   more comfortable, which lets you attract them at a lower headline rate.\n\nThe honest sequencing: **seed + incentivize to get initial liquidity and borrowers → real\nutilization builds → the dynamic curve takes over and you dial incentives down.**\n\n## 5. What Assay needs to implement (current state → target)\n\n**Today:** the pool stores a single fixed `rate_bps` (set to 0 in the demo, so there's no interest —\nthat's why the demo shows ~0% APY). The accrual math (`accrue`) already compounds a rate over time;\nit just uses a constant.\n\n**Target:** make the rate a **function of utilization** instead of a constant. Concretely, in the\nMove contract:\n- Store the curve params in the pool: `base_bps, slope1_bps, slope2_bps, kink_bps, reserve_bps`.\n- In `accrue`, compute `U` from `liquidity` vs `total_borrows`, evaluate the kinked borrow APR, and\n  compound *that* over the elapsed time (instead of the fixed `rate_bps`).\n- Route `reserve_bps` of the interest to a protocol reserve balance.\n- Supply APY is then **emergent** — the UI already derives it as `rate × utilization` (see\n  `usePool` / the Earn panel), so it will display correctly once the rate is dynamic.\n\nThat's a contained contract change (no new external dependencies, no oracle changes). It converts\nthe pool from \"fixed demo rate\" to a real, self-balancing money market — and it's the piece that\nmakes \"deposit USDC and earn yield\" actually mean something.\n\n---\n\n### TL;DR\n- Don't set a rate — set a **curve**; utilization sets the rate automatically and keeps the pool liquid.\n- Target a **supply APY that beats ~5–10% alternatives** at your expected utilization; work backwards\n  to the borrow curve (example: ~14% borrow at 80% util → ~10% supply).\n- The rate model doesn't solve cold-start — **seed the pool + incentivize early supply**, then let the\n  curve take over.\n- Implementation is a **contained contract change**: make `accrue` compute the rate from utilization\n  (kinked curve) instead of a constant, add a reserve factor. The UI already shows the emergent APY.\n"},{"slug":"outstanding","group":"Risk","file":"OUTSTANDING.md","title":"Known-open items","desc":"Everything we know is unfinished, including what blocks mainnet. Published deliberately.","md":"# Outstanding\n\nEverything known-open, as of 2026-07-20. Written down so none of it lives only in someone's head.\n\n---\n\n## Blockers before any mainnet deployment\n\n**1. No round has ever come back clean.** The stated gate is three auditors clean in the same\nround. Six Move rounds and one Solidity round; none met it. Severity is falling and the remaining\nitems are mediums, but the gate is unmet and the code is unpushed-to-production for that reason.\n\n**2. The ZK layer has never been audited, and cannot be.** `circuit/` contains a Poseidon gadget\nand **no constraint system**. There is no `.zkey`, `.r1cs`, `.ptau` or `.wasm` anywhere. Audit\nfindings F1 and F4 both turn on what the batch proof binds, so the most load-bearing component in\nthe \"provably safe\" claim is the one nobody has been able to check.\n\n**3. Both circuits must be re-proven upstream.** `dregg_lending` cannot originate and\n`settle_batch` cannot succeed until they are. This is deliberate — a lending function that can be\ndrained must not originate loans — but it is blocked outside this repo.\n`perloan-prep/RUNBOOK-terms-binding.md` still specifies the **pre-fix 8-term preimage**; it needs\nthe collateral-type term or the on-chain binding will never match.\n\n**4. ~~`README.md` claims \"provably safe.\"~~ FIXED.** The README now leads with a \"What is\nactually proven today\" table separating the design's goal from what the deployment enforces, and\nthe strongest claims in `WHY-ASSAY-IS-DIFFERENT.md` and `ARCHITECTURE.md` were softened to match.\nRe-tighten the language as the circuits land and are audited — the table is meant to change.\n\n**5. Republish required.** `Pool`, `Position` and `OperatorCap` all changed layout, so Sui cannot\nupgrade in place. Needs a fresh package, a fresh pool, and a new `VITE_POOL`.\n\n---\n\n## Open findings\n\n### Move (`move/`)\n\n| Severity | Item |\n|---|---|\n| high | `disburse_entry` hands a cap holder the whole `pool.cap` against zero collateral — no signature, no delay, no pause check. **A trust-model decision, not a patch:** is the cap holder trusted with the full pool, or does that path need an attestation too? |\n| medium | `repay` demands exact equality against a debt that grows every second, with no on-chain debt view to size it from. Fixed by construction in the Solidity port; not backported. |\n| low | Faucet rate limit keyed on the raw request string, bypassable by address casing/padding. Devnet only. |\n\n### Solidity (`rh-chain/`)\n\n| Severity | Item |\n|---|---|\n| critical | A watched token returning a non-boolean `paused()` word makes `abi.decode` panic inside `accrue()`, freezing **every** entry point — including liquidation — until a single admin EOA resets the array. |\n| high | Pro-rata collateral is correct on CLOSE but wrong on OPEN: the denominator is live, so anyone who borrows **after** a burn is charged for it. |\n| high | The decimals fix moved the trust into two operator-typed fields that nothing cross-checks against the token's real `decimals()`. A one-character typo reproduces the original 1e12 drain. |\n| high | `accrue()` runs **after** OZ computes the share price in all four ERC4626 paths, so pending interest is extractable — and flash-loanable. |\n| medium | Pause suspends interest **pool-wide** and forgives it permanently, so an unrelated token's pause hands every borrower a free loan. |\n| medium | DST intersection does not handle the ~3 half-day early closes per year. |\n| medium | The holiday guard false-rejects on EDT days whose only print lands in the 13:30–14:30 opening hour — and because `canLiquidate` shares the flag, that is a liquidation outage. |\n| medium | `resumeGrace` at 6× `gapThreshold` turns routine keeper jitter into a permanent liquidation DoS. |\n| medium | ~50 mutations survive a green suite, including `MIN_RISK_GAP_BPS` and `PARAM_TIMELOCK`. |\n\n**Round 2 was not clean.** This is the fourth consecutive round in which a shipped fix either did\nnot work or introduced a new defect, and the third in which a claim I made in a commit message or\ndocument was disproven by the next round.\n\nNothing in `rh-chain/` is deployed to any network.\n\n---\n\n## Operational, before mainnet\n\n- **Deploy the `LivenessOracle` keeper** under a supervisor with alerting. It exists and is tested;\n  it is not running. A silently dead keeper degrades to \"liquidations off\" — safe, but an outage.\n- **Split the keys.** The keeper's hot key must not be the cold guardian key, and the Sui keeper\n  currently co-locates the `OperatorCap` and the operator signing key in one process, which\n  collapses a two-party control into one.\n- **Sequencer uptime feed.** Robinhood's docs say Chainlink provides one for Robinhood Chain; it is\n  not on Chainlink's canonical list, not in the feed directory, and every contract from Chainlink's\n  deployer on that chain resolves to a price feed. Either locate it (ask\n  `chain-developers-group@robinhood.com`) or keep running on the keeper.\n- **Verify who holds `ADMIN_BURNER_ROLE`.** On-chain it is a plain EOA with no multisig and no\n  timelock — one key can destroy collateral inside a live pool. That is unmitigated on-chain and\n  should be priced into LTV and disclosed to borrowers.\n\n---\n\n## Open questions\n\n1. Can Robinhood's Trading MCP place the equity orders that become Stock Tokens, in the\n   jurisdictions we care about? The agent half of the MVP is untested end-to-end.\n2. Chainlink feed heartbeats are 86400s / 0.5% today. `rh-chain/script/fetch-feeds.mjs` fails loudly\n   if that changes — but nothing runs it on a schedule yet.\n3. Has `adminBurn` ever actually been used? Recoverable from `Transfer`-to-zero logs; frequency\n   decides whether it is theoretical or operational.\n"},{"slug":"audits-readme","group":"Audits","file":"audits/README.md","title":"How we audit","desc":"Independent agents attack every change; findings are published fix-first, clean or not.","md":"# Security audits\n\nEvery substantive change to this protocol goes through an adversarial audit before it is\nconsidered done: independent agents attack the code from different angles, and **every finding is\nthen independently verified by two skeptics whose job is to refute it**. Only findings that\nsurvive refutation are reported. Refuted findings are recorded too — they show what was considered\nand why it was dismissed, which is often more informative than the confirmed list.\n\n## Publishing policy\n\nThese reports are public deliberately. The discipline is **fix-first**:\n\n- A finding's full detail — exploit path, code, fix, closing commit — publishes **after** its fix\n  is committed.\n- **Open** findings appear as a placeholder: round, severity, affected surface, status. No exploit\n  path.\n- Clean rounds and refuted findings publish immediately; neither arms anyone.\n\nThe point is a trail an outsider can actually check, not a highlight reel. Severities are the\nverifiers' calibrated ones, not the reporters' initial claims, and the reports name what was NOT\ncovered.\n\n## Reports\n\n| Round | Target | Confirmed | Status |\n|---|---|---|---|\n| [Sui 1–6](sui-rounds-1-6.md) | `move/` — the Move protocol | 9, 10, 17, 9, 9, 12 | fixes committed; **no round ever came back clean** |\n| [Solidity 1](solidity-round-1.md) | `rh-chain/` — the Robinhood Chain port | 19 | criticals + highs fixed; tail open |\n| [Solidity 2](solidity-round-1.md#round-2) | `rh-chain/` — re-audit + mutation sweep | see round-1 report | **not clean**; ~50 mutations survive a green suite |\n\nRound 2 has no separate file: its findings are corrections to round 1 and are folded into that\nreport. It is listed separately here because the round-1 document, read alone, states a\nmutation-coverage claim that round 2 disproved by a factor of 50 — including that\n`MIN_RISK_GAP_BPS` and `PARAM_TIMELOCK`, the two constants we cite publicly as controls, can both\nbe halved with every test passing. Anyone auditing us should know that before trusting round 1's\ncoverage section.\n\n## Method\n\nEach round runs three auditors over different surfaces, then two verifiers per finding — one\ninstructed to **refute**, one to **reproduce**. Verifiers write and run real PoCs (Move tests,\nFoundry tests, on-chain `eth_call`) rather than reasoning. Roughly a third of initial findings do\nnot survive.\n\nAlongside every round we mutation-test guards: delete the guard, and a test must fail. We have\ntwice claimed \"all guards mutation-verified\" and been wrong both times — first by only mutating\nguards we had already written tests for (circular), then by using a sweep script that matched\n`if (...) revert` and was structurally blind to early-return guards, reporting 1 survivor where an\nindependent sweep of 139 mutations found 50. A passing suite is necessary and nowhere near\nsufficient, and our own coverage claims are the thing to check hardest.\n"},{"slug":"audits-sui-rounds-1-6","group":"Audits","file":"audits/sui-rounds-1-6.md","title":"Move / Sui — rounds 1–6","desc":"66 confirmed findings across six adversarial rounds. No round came back clean.","md":"# Move / Sui protocol — adversarial audit rounds 1–6\n\n**Target:** `move/` · **Date:** 2026-07-19–20 · **Result:** 66 confirmed across six rounds ·\n**No round ever came back clean.**\n\n| Round | Audited | Confirmed | Ceiling |\n|---|---|---|---|\n| 1 | the codebase as it stood | 9 | 2 critical |\n| 2 | the F2 fix | 10 | 1 critical |\n| 3 | the R2 fixes | 17 | 2 critical |\n| 4 | the R3 fixes | 9 | high |\n| 5 | F1/F3/F4 structural fixes | 9 | high |\n| 6 | the R5 fixes | 12 | high |\n\nSeverity fell steadily; findings did not stop. Rounds 5 and 6 are the honest ones to read, because\nin each the highest-severity finding was **introduced by the previous round's fix**.\n\n---\n\n## Fixed (full detail — see the commits)\n\n### F1 CRITICAL — the proof proved nothing about the loan (`dregg_lending::borrow`)\nGated on a nullifier and a Groth16 verify. `debt` was a free caller-supplied `u64`; collateral was\nconsumed without a single `coin::value` inspection. Nothing tied debt, collateral, sender or pool\nto what the proof proved. `loan_commit_of` computed the correct binding and had **zero call sites**.\n\nA verifier reproduced it with the repo's own fixture: any holder of one valid `(payment_id, proof)`\n— *including an honest borrower issued a proof for a 50 USDC loan* — could call with\n`debt = pool_liquidity()` and `coin::zero()` collateral and empty the pool, then replay against\nevery other pool pinning the same vk.\n\n**Fix (`45287d9`, extended in `7d8584b`):** the proof's public input must equal a commitment over\nthe actual pool, sender, debt, collateral **amount and type**, LTV and nonce. Round 5 caught that\nthe first version bound the amount but not the **type** — a proof for 100e9 of a real RWA was\nredeemable with 100e9 of a worthless coin the attacker publishes themselves.\n\n### F2 CRITICAL — the operator attestation was a perpetual bearer instrument\nNo expiry, no nonce, no consumed-set, no pool binding — and it was the *only* solvency gate, since\nthe module has no on-chain oracle. Poll `/borrow` at a price peak, hold the signature through a\ndrawdown, redeem a peak-priced loan that is insolvent at origination. Replay compounded it.\n\n**Fix (`6d9046a`):** expiry with a 120s ceiling, a `used_commits` nullifier, pool and stable-type\nbinding, and key rotation. Type names are BCS length-prefixed — raw concatenation would make\n`(\"AB\",\"C\")` and `(\"A\",\"BC\")` identical bytes.\n\n### F3 HIGH — liquidation checked nothing and seized everything\nThe doc comment claimed operator-attested, underwater-only liquidation. The body verified no\nattestation, read no price, checked no health condition — the only gate was an **unused** `_cap`\nparameter — and it seized 100% of collateral with no refund. A cap holder could liquidate a\nperfectly healthy position at ~1.43×, repeatedly; and a position 1bp underwater forfeited\neverything even with an honest operator.\n\n**Fix (`d2f04fc`):** a signed, amount-bound, short-lived attestation plus surplus refunded to the\nborrower. Adding a second signed message type also required domain separation — a refuted round-2\nfinding that became real the moment the code changed around it.\n\n### F4 HIGH — `settle_batch` was permissionless and replayable\nPublic input was `bcs(batch_root)` alone. Groth16 is stateless, so a scraped proof could be\nreplayed to reset `total_pending` — the only global cap on unproven exposure — indefinitely.\n**Fix (`f70a9ec`):** cap-gated, public input binds pool id and batch index, and the accumulator\nfolds unconditionally from `acc_0 = 0`.\n\n### R2-1 CRITICAL — `OperatorCap` was not bound to a pool\n`init_pool` is permissionless, so anyone could mint a cap and point it at someone else's pool.\nNone of seven cap-gated functions inspected it. A PoC drained a funded pool against 1 unit of a\nself-minted worthless coin and seized 100,000 units from a healthy position.\n\n### R2-2 HIGH — an unbounded rate curve bricked the pool permanently\nAn absurd `base_bps` made the accrual downcast abort, and every entrypoint calls accrue first —\n**unrecoverable even by the operator**, since the repair transaction accrues before applying the\nfix. A plain typo reached it.\n\n### R2-3 / R4 — the low-order ed25519 key check, wrong twice\n`0x00×32` is a low-order point and Sui's ZIP-215 verify **accepts any signature against it**,\nturning the only solvency gate into a no-op. The first fix substituted two non-canonical encodings\nfor two genuine order-8 points, leaving both forgeable keys accepted. The second enumeration was\nstill incomplete — 4 of 14 valid encodings missing.\n\n**Fix:** stopped enumerating. A canonical key encodes `y < p`, so all non-canonical forms are\nrejected structurally, and all 14 encodings are asserted by test.\n\n### R3, R5, R6 — the binding class, one level at a time\n`Position` was not bound to its pool (R3). `total_pending` was released only by `settle_batch`,\nwhich F4 had made impossible, so it accumulated over lifetime volume until `pool.cap` bricked all\nborrowing (R5) — and the fix for that then **double-released** against `settle_batch`'s wholesale\nreset, making the cap stop binding entirely (R6).\n\n---\n\n## Open\n\n| # | Severity | Surface | Status |\n|---|---|---|---|\n| 1 | high | cap-holder trust model on the un-attested disburse path | open — design decision, not a patch |\n| 2 | medium | `repay` exact-equality with no on-chain debt view | open |\n| 3 | low | faucet rate-limit bypass | open |\n| 4 | — | both circuits must be re-proven upstream | blocked externally |\n\nThe deployed devnet package predates these fixes and holds faucet-minted test coins only.\n\n---\n\n## What this actually cost, and bought\n\nThree of the fix commits contained **claims the next round disproved**:\n\n- F1's message said it closed \"the collateral substitution.\" It closed *amount* substitution only.\n- F3's said liquidation now took two parties. It did not — rotation and liquidation shared one cap\n  with no delay, so both could happen in a single transaction.\n- F4's said exposure was \"still released by repay/liquidate.\" That was a different ledger. The one\n  in question was released by nothing.\n\nFour guards were deletable with a fully green suite, including `EOverCap` — the exact ledger round 5\nexisted to preserve. `loan_commit_of`'s Poseidon preimage could be **reordered**, or its hash\nfunction swapped, with every test still passing.\n\nThe output that survived is the invariant list, and it is the thing that ported to Solidity when\nthe implementation did not:\n\n1. Authorisation must bind the loan's actual terms — debt, collateral **amount and type**.\n2. Liquidation needs a real health check and must refund surplus.\n3. Every capability must be bound to the object it governs.\n4. Exposure must release on close, exactly once, from exactly one place.\n5. Rate parameters must be bounded; arithmetic must saturate, never trap funds.\n6. Privileged changes need a timelock, so rotate-and-use is not atomic.\n7. A guard is not covered until you have deleted it and watched a test fail.\n\n## Not covered\n\nThe ZK layer was never audited — `circuit/` contains a Poseidon gadget and no constraint system,\nand no `.zkey`/`.r1cs`/`.ptau` exists in the repo. Since F1 and F4 both turn on what the proof\nbinds, **the most load-bearing component is the one nobody could check.** The dregg kernel lives\noutside this repo and was not reviewed.\n"},{"slug":"audits-solidity-round-1","group":"Audits","file":"audits/solidity-round-1.md","title":"Solidity — round 1","desc":"19 confirmed, 5 refuted against the Robinhood Chain port. Not clean.","md":"# Solidity port — adversarial audit round 1\n\n**Target:** `rh-chain/` (AssayPool, AssayMarkets, StaleFeedGuard, LivenessOracle,\nCollateralReconciler) · **Date:** 2026-07-20 · **Result:** 19 confirmed, 5 refuted · **Not clean.**\n\nThree auditors, two verifiers per finding, PoCs executed against the real contracts.\n\n---\n\n## The finding that matters most\n\n> Move's resource model makes collateral a non-fungible owned object, so a position *physically\n> cannot* be paid out of another position's asset. Solidity commingles ERC-20 balances, and the\n> port translated `min(position, pool_balance)` literally.\n\nThe Move predecessor went through six audit rounds. **Every invariant those rounds produced was\ncarried across — and the invariants Move enforced for free were not.** The type system had been\ndoing safety work the port did not know it was relying on. That single observation explains two of\nthe four criticals.\n\n---\n\n## Fixed (full detail — fixes committed in `05a59cb`)\n\n### CRITICAL — collateral valued in the wrong decimals, 1e12 over-borrow\n`AssayMarkets.collateralValue` divided out only the *feed* decimals and returned a\ncollateral-scaled number, which was then compared against a 6-decimal USDG debt. Every LTV limit\nwas 1e12 too permissive: a $2,000 AAPL position could drain the entire lender pool, and the\nresulting position was not even liquidatable because `isUnderwater` made the same comparison.\n\nVerified on mainnet: USDG `6`, AAPL Stock Token `18`, Chainlink feed `8`.\n\n**Why no test caught it:** the suite's `MockUSDG` used 18 decimals. A mock that differs from\nproduction hides exactly the bugs production has.\n\n**Fix:** normalise to the borrow asset — `value = uiAmount × price × 10^assetDec / (10^collDec ×\n10^feedDec)`; `collateralDecimals` is now a required, validated market parameter; the mock is 6.\n\n### CRITICAL — `adminBurn` loss allocated by repayment order\n`recordedRaw` was per-**token** while positions are per-**borrower**, and the code clamped a\nper-borrower entitlement against the pooled balance. Alice and Bob each post 10 units; Robinhood\nburns 10; **whoever repays first recovers all 10 — including the other's** — and the second\nrecovers nothing despite paying their debt in full.\n\nThe shortfall machinery meant to prevent this was inert: `shortfallRaw` written and never read,\n`ShortfallSocialised` declared and never emitted, all three `_reconcile` call sites discarding the\nreturn value. The contract's own comments described a policy the code did not implement.\n\n**Fix:** `_effectiveCollateral` scales each position by `surviving / nominal`, socialising the loss\npro-rata. Alice and Bob each get 5, in either order.\n\n### HIGH — first-depositor share inflation\nHand-rolled 4626 maths with no virtual offset, no dead shares, no minimum-shares floor, and a\ndonatable `totalAssets()`. Deposit 1 wei, donate to inflate the share price, and the victim's\ndeposit rounds to **zero shares** while the attacker redeems everything.\n\n**Fix:** OpenZeppelin `ERC4626` with `_decimalsOffset() = 6`. The attack is now written out as an\nattack in the test suite, not asserted as prevented — see the mutation note below for why that\ndistinction mattered.\n\n### HIGH — `adminBurn` made an unsecured position permanently unliquidatable\nThe health check read the stored `collateralRaw`, never decremented on a burn, so a position\nbacking nothing read as healthy and could never be liquidated. **Fix:** health and seizure both use\nthe surviving balance.\n\n### HIGH — `borrow(debt = 0)` trapped collateral forever\nNo exit path: `repay` reverts `NoDebt`, and `isUnderwater` is false at zero debt so liquidation\nalso refuses. **Fix:** zero debt rejected at origination.\n\n### HIGH — pause-aware accrual was documented and never implemented\nThe scope doc names it a requirement; `grep -rn 'paused' src/` returned only comments. A Robinhood\ntoken pause blocks transfers, so a borrower cannot repay — and interest kept compounding across a\nwindow in which repayment was impossible, on the issuer's pause rather than theirs. **Fix:**\naccrual suspends while a watched collateral token is paused.\n\n---\n\n## Open (placeholder — detail publishes when fixed)\n\n| # | Severity | Surface | Status |\n|---|---|---|---|\nAll four mediums are now **fixed** — detail below. Nothing in `rh-chain/` is deployed to any\nnetwork, so none of this was ever live exposure.\n\n### MEDIUM — the session calendar was an hour wrong half the year (fixed)\n`isUsMarketHours` hardcoded the EST mapping (14:30-21:00 UTC). During EDT that reported\n\"in session\" for the hour AFTER the close — the unsafe direction, admitting borrowing into a shut\nmarket. **Fix:** the window is now the INTERSECTION of the EST and EDT mappings, 14:30-20:00 UTC,\nso it is never open when the market is shut. It gives up an hour of availability; a full DST\nimplementation would recover it.\n\nThe existing boundary test asserted 20:59 UTC was in-session, which is only true under EST — the\ntest **enshrined the bug it should have caught**. Rewritten.\n\n### MEDIUM — market holidays read as in-session (fixed)\nThe calendar knows weekends, not holidays. On Thanksgiving the clock says in-session while the feed\nhas not printed since the previous close, and an 18-24h holiday gap fits inside the 25h staleness\nbound, so staleness could not catch it. A source comment claimed it did. **Fix:** if the clock says\nin-session, the feed must have printed at or after **today's** open; otherwise the session is\ntreated as closed. A very quiet stock is also refused, which is the conservative direction.\n\n### MEDIUM — short outages slipped past the liveness gate (fixed)\n`LivenessOracle` used `maxHeartbeatAge` as both the liveness bound and the gap detector, so an\noutage shorter than it was invisible: the heartbeat never went stale, no gap was recorded, and\nliquidations resumed in the first block back — the exact restart-liquidation the contract exists to\nprevent, at a smaller scale. **Fix:** a separate, tighter `gapThreshold`, validated to be below the\nliveness bound.\n\n### MEDIUM — surviving guard mutations (PARTIALLY fixed; the claim made here was wrong)\nA sweep found 13 guards deletable with a green suite, and twelve were given tests. Two of those\nwere not missing tests but **bare `expectRevert()` calls**, which match any revert including the\none that would happen anyway.\n\n**Round 2 disproved the coverage claim by a factor of 50.** An independent sweep of 139 mutations\n— deletions, comparison inversions, operand swaps and constant changes across all seven source\nfiles — found **50 survivors** on the same green suite.\n\nThe cause was a bug in the measurement tool, not merely its narrowness: the sweep matched only\n`if (…) revert` and `require(…)`, so it was structurally blind to the **early-return** guard form\n(`if (x == 0) return 0;`), which is used throughout. Two survivors fall inside the sweep's own\nstated scope. **A claim about coverage was published on the output of a tool that had never been\ntested.**\n\nCorrected status: 12 guards gained tests; roughly 50 mutations survive; the constants the README\nnames as the controls (`MIN_RISK_GAP_BPS`, `PARAM_TIMELOCK`) can be halved with the suite green.\n\n---\n\n## The uncomfortable part: mutation testing\n\nBefore this audit the author reported *\"63 tests, all guards mutation-verified.\"* **That was\nfalse.** The guards mutated were the ones tests had already been written for — which is circular,\nand can only confirm what was already thought of. The audit mutated all 65 and **18 survived a\nfully green suite**, including a ported audit invariant (the per-market exposure cap) and an admin\ncheck.\n\nIt repeated during the fixes: of six, five caught their mutation immediately and the ERC4626\ninflation protection did **not** — the mitigation was present and nothing proved it worked.\n\nThe rule that came out of it: **a guard is not covered until you have deleted it and watched a test\nfail.** Anything else is an assumption wearing a test's clothes.\n\n---\n\n## Refuted (recorded so they are not re-litigated)\n\n- **Multiplier changing between health check and seizure** — impossible; `block.timestamp` is fixed\n  within a transaction, so both reads see the same value.\n- **`1e18` denominator for `uiMultiplier` assumes 18-decimal tokens** — it does not; the constant\n  belongs to `ERC20ScaledUIUpgradeable` and is independent of token decimals.\n- **Forward-split mispricing** — handled correctly; only the reverse-split direction is exposed, and\n  via feed lag rather than the multiplier.\n- **Fee-on-transfer / non-standard `balanceOf`** — mechanically real if Robinhood upgrades the\n  shared beacon, but not attacker-triggerable.\n- **Dead/duplicated valuation helpers** — accurate observation, no reachable defect.\n\n## Not covered\n\nNo fork testing against real Stock Tokens; no gas/DoS analysis; no formal verification; the keeper\nservice and deploy scripts were out of scope.\n"},{"slug":"audit-2026-07-14-sui","group":"Audits","file":"AUDIT-2026-07-14-sui.md","title":"Security audit & pentest","desc":"Adversarial review + penetration test — one critical bug found and fixed.","md":"# Security audit + penetration test — Assay Sui stack (2026-07-14)\n\n> **Chronology — read this in context.** This is an early, narrower audit dated 2026-07-14. It was\n> superseded by six full adversarial rounds on 2026-07-19–20 that found **66 further confirmed\n> issues**, and by a first Solidity round on the Robinhood Chain port that found **19**. A clean\n> result here does not mean the code was clean; it means this round's scope was smaller. Start at\n> [audits/README.md](audits/README.md) for the current picture and\n> [OUTSTANDING.md](OUTSTANDING.md) for what is still open.\n\n\nAdversarial review of the Sui-native contract (`async_lending.move`), the operator API, and the\nweb client, plus a live on-chain penetration test. Scope included the new features shipped this\nsession (dynamic rates, governance, per-collateral isolation).\n\n**Headline: one CRITICAL vulnerability found and fixed; two hardening items; 8/8 live attacks blocked.**\n\n---\n\n## CRITICAL — collateral-type substitution (FOUND + FIXED)\n\n**Where:** `disburse_attested<Collateral, Stable>`.\n\n**The bug:** the operator's ed25519 attestation signed `borrower ‖ debt ‖ collateral_amount ‖\nloan_commit` — it bound the collateral **amount but not its type**. The `Collateral` type is a\ngeneric parameter chosen by the *caller*. So the on-chain check verified \"the operator approved a\nloan of N units of *something* for this debt\" — not *which* asset.\n\n**The exploit (real, direct fund loss):**\n1. Attacker requests a legitimate quote for a valuable collateral — e.g. 1 cbBTC (1e8 units) →\n   operator authorizes borrowing 4,000 USDC and signs the attestation.\n2. Attacker deploys their own worthless coin `FAKE` (freely mintable) and mints 1e8 units.\n3. Attacker calls `disburse_attested<FAKE, TUSDC>` with the FAKE coin + the *same attestation*.\n4. `coin::value == 1e8` matches, the signature verifies (type wasn't signed), and they borrow\n   **4,000 real USDC against worthless FAKE collateral.** Never repaid → bad debt → pool drained.\n\n**The fix:** bind the collateral type into the signed message. The operator now signs the exact\ncollateral type; the contract reconstructs it in-Move via `type_name::get<Collateral>()` and appends\nit to the verified message:\n```\nmsg = bcs(borrower) ‖ bcs(debt) ‖ bcs(coll_amt) ‖ bcs(loan_commit) ‖ type_name(Collateral)\n```\nNow presenting a different coin type produces a different message → the signature fails →\n`EBadAttest`. The off-chain SDK reconstructs the identical canonical type string\n(`normalizeStructTag(coinType)` with the `0x` stripped), verified to byte-match `type_name` by a\n**successful legit on-chain loop** (proving the fix doesn't break the real path).\n\n**Verified:** pentest **P8** (\"sig for SSPX, present TUSDC, same amount, everything else identical\")\nis now **BLOCKED** on-chain; SDK unit test `collateral-substitution is blocked`; legit type-bound\ndisburse still succeeds end-to-end through the hosted operator.\n\n---\n\n## Hardening (defense-in-depth)\n\n- **`reserve_bps` unbounded** → a bad governance value `> 10000` would make the reserve skim exceed\n  interest and could break accrual math. **Fixed:** `assert reserve_bps ≤ 10000` at init + on every\n  `set_rate_curve`. (Governance-trusted, but bounding it removes a foot-gun.)\n- **First-depositor share inflation (ERC-4626 class)** — known, pre-existing, MEDIUM. There is no\n  public \"donate to liquidity\" function, so the only way to inflate share price without minting\n  shares is accrued interest; and the live pool is seeded with a large first deposit (5,000 USDC),\n  which makes the rounding attack impractical. **Mitigation deferred** (dead-share seeding) — tracked,\n  not newly introduced.\n- **Attestation replay** — an attestation has no on-chain single-use marker, so the same one could\n  open multiple identical loans. LOW: each replayed loan is *fully collateralized with the now-type-\n  bound collateral*, so no pool loss — it only lets a borrower take more identical safe loans than\n  intended. Candidate for a used-`loan_commit` set later.\n\n---\n\n## Penetration test — 8/8 blocked (live devnet, `app/sui-sdk/test/pentest-sui.mjs`)\n\n| # | Attack | Result | Guard |\n|---|--------|--------|-------|\n| P1 | forged attestation (random sig) | 🛡 BLOCKED | `EBadAttest` (ed25519) |\n| P2 | tampered terms (sig 500, submit 501) | 🛡 BLOCKED | `EBadAttest` (msg binds every term) |\n| P5 | repay another's position (seize collateral) | 🛡 BLOCKED | `ENotBorrower` |\n| P6 | over global cap disburse | 🛡 BLOCKED | `EOverCap` |\n| P7 | exceed per-collateral isolation cap | 🛡 BLOCKED | `EOverCollateralCap` (new) |\n| **P8** | **collateral substitution (worthless type)** | 🛡 **BLOCKED** | **`EBadAttest` — type binding (the fix)** |\n| P3 | junk-collateral borrow (unregistered) | 🛡 BLOCKED | operator registry 400 |\n| P4 | over-LTV / unauthorized borrow | 🛡 BLOCKED | operator refuses |\n\nAlso reviewed, no issue found: share math (withdraw ≤ owned + ≤ cash), reserve accounting\n(lenders' claim = cash + borrows − reserves, floored at 0), governance gating (`OperatorCap`-only for\n`set_rate_curve` / `set_collateral_cap`), utilization overflow (u128 intermediate), the P5 borrower\ncheck on repay, and the on-chain proof gate on `settle_batch`.\n\n**Contract:** `sui move test` 9/9. Deployed fixed contract: v8\n`0x226cb61426fbc855a1edddfd1c0913a64eb47a4c071f59baa08098003267df20`.\n\n---\n\n## Honest takeaways\n- The audit did its job: an adversarial pass on freshly-written code found a **real, critical,\n  directly-exploitable** bug (undercollateralized borrow via type substitution) that unit tests +\n  the happy path had missed — exactly the class of thing audits exist to catch.\n- The formally-verified dregg kernel enforces the *LTV/solvency* invariant; but the **attestation\n  bridge between the operator and the chain is ordinary code**, and that's where this bug lived. It's\n  a reminder that \"provably safe\" covers the proven component, not the glue — the glue needs audits.\n- Remaining known items (first-depositor inflation, attestation single-use) are documented, bounded,\n  and not fund-loss on the live seeded pool; they're the honest backlog before mainnet.\n"},{"slug":"evidence-2026-07-14","group":"Audits","file":"EVIDENCE-2026-07-14.md","title":"Evidence","desc":"On-chain proofs, the penetration test, and full end-to-end loans.","md":"# Evidence — penetration test + 3 full loans (2026-07-14)\n\n> **Chronology — read this in context.** This is an early, narrower audit dated 2026-07-14. It was\n> superseded by six full adversarial rounds on 2026-07-19–20 that found **66 further confirmed\n> issues**, and by a first Solidity round on the Robinhood Chain port that found **19**. A clean\n> result here does not mean the code was clean; it means this round's scope was smaller. Start at\n> [audits/README.md](audits/README.md) for the current picture and\n> [OUTSTANDING.md](OUTSTANDING.md) for what is still open.\n\n\nReproducible: `bash app/operator-api/run-pentest.sh` — spins up a validator, deploys the\nprogram, starts the Operator API, runs the live attack suite, then 3 full loans.\n\n## Penetration test — 7/7 attacks blocked\nLive on-chain exploit attempts against a real deploy + the Operator API (not a review —\nactual attacks). Each SHOULD fail; \"blocked\" = rejected.\n\n| Attack | Targets | Result | Why |\n|--------|---------|--------|-----|\n| A1 fake-vault share-inflation drain | CRITICAL-1 | 🛡 BLOCKED | `require_pool_vault` — non-canonical vault errors `0xE2` |\n| A2 junk-collateral borrow (unregistered mint) | CRITICAL-2 | 🛡 BLOCKED | server registry rejects unknown mint (400) |\n| A3 over-LTV borrow (debt ≫ max) | LTV enforcement | 🛡 BLOCKED | server max-check + dregg (403) |\n| A4 repay another user's position | access control | 🛡 BLOCKED | `pos_borrower != borrower` → EWRONGACCT |\n| A5 over-withdraw (more shares than owned) | share math | 🛡 BLOCKED | EMATH |\n| A6 self-disburse as fake operator | operator gate | 🛡 BLOCKED | `!= config.operator` → ENOTOP |\n| A7 tamper operator-signed tx (append instruction) | partial-sign integrity | 🛡 BLOCKED | operator sig doesn't cover the changed message → \"Signature verification failed\" |\n\n**7/7 blocked. No exploit succeeded.**\n\n## Three full loans — complete flow, on-chain\nEach: `/quote → dregg authorize → operator co-sign → borrower sign → position → repay →\ncollateral returned → position closed`. (Localnet tx signatures — real signed+confirmed\ntransactions; ephemeral ledger, so re-run the harness or use devnet for public explorer\nverification.)\n\n| # | Collateral | Price | Borrowed | Repaid → | Borrow tx | Repay tx |\n|---|-----------|-------|----------|----------|-----------|----------|\n| 1 | 1 unit | $64,917.27 | $6,000.00 | collateral returned, closed | `rAxPfEu6…KMQe7X` | `LXMN3aU…v39M5` |\n| 2 | **0.5 unit** | $64,926.59 | $2,594.76 | collateral returned, closed | `GXVPaQg…oZacs` | `2kASRAt…RZ1WzD` |\n| 3 | 2 units | $64,935.50 | $6,000.00 | collateral returned, closed | `zfcyAUV…aji9SW` | `5xfm8U2…kicnD` |\n\n**3/3 completed.** Loan #2 (fractional 0.5-unit collateral) confirms the fractional-collateral\nfix — the kernel authorizes against the true 0.5 units, not a rounded 1.\n\n## Note on verifiability\nThese ran on a local validator (deterministic, reproducible), so the signatures are real but\nthe ledger is ephemeral. For third-party-verifiable evidence on a public explorer, run the\nsame flow on **devnet** (one config flip + ~3 devnet SOL — funding-gated) or mainnet. The\nharness (`run-pentest.sh`) is the durable proof: anyone can run it and see 7/7 blocked + 3/3 loans.\n\n## Proof pushed on-chain — Sui devnet (publicly verifiable)\nThe dregg proof is not just enforced in our program logic — the real BN254 Groth16 proof\n(`perloan-prep/proof_A.json`, the STARK→apex→Groth16 wrap) is **verified natively on Sui**\nvia `sui::groth16`. Package `dregg_verifier` @ `0x1ef488bbf6afe48c2e0236075c94a0dc6b5b017946bdade77f99f1a23bcf0478`.\n\n| Case | Input | Result | Tx digest |\n|------|-------|--------|-----------|\n| Valid proof | real `proof_A` (vk+publics+proof) | ✅ **success**, emits `Verified{ok:true}` | `DaATa2B2iDdPupHKioKcUwBPBHmJ1nSFYdDniVVXTnZS` |\n| Tampered proof | same, 1 byte flipped (`d4`→`d5`) | 🛡 **aborted** `EProofInvalid` (57005 / 0xDEAD) | `6bYtRnjN7gf8bVbffjQUTaK7EdyHWTLrjNhmXtBeadSL` |\n\n`assert_verify` aborts unless `sui::groth16::verify_groth16_proof` returns true, so a **successful\ntransaction *is* the on-chain proof of verification** — and the negative control shows the chain\ngenuinely checks it (a single flipped byte is rejected). These are real Sui devnet transactions\n(explorer-verifiable now; devnet state may be pruned on the next reset — mirror to testnet/mainnet\nfor permanence). Verifier source: `onchain-sui/move/dregg_verifier/sources/verifier.move`.\n\n## Full lending loop on-chain — Sui devnet (the Sui-native twin of the 3 loans)\nThe entire lending lifecycle runs on Sui, on-chain, with real tx digests — mirroring the Solana\n\"3 loans\" but Sui-native. Contracts live on devnet: `async_lending` pkg\n`0xf05ad39908244ae7312a7fea808ec527e42b63b0c0cc2d39679ed1ce1d777a22` (bundles `dregg_verifier`).\nReproducible: `bash app/sui-harness/loop.sh`. Test coins pkg `0x537ba694…` (`TUSDC` 6-dec stable,\n`SSPX` 8-dec test xStock collateral).\n\n| Step | Action | Tx digest |\n|------|--------|-----------|\n| 1 | mint 1000 TUSDC (LP) | `BaJbv2TGBjmRJbptEFDS4QitHPm28ZxZWMqR7z6Wh2fs` |\n| 1 | mint 100 SSPX (collateral) | `8U5ARjrLjn5GfS27pehxyNHSMxbsQz5UerpSG5EwjExy` |\n| 2 | `init_pool<TUSDC>` (share Pool + OperatorCap) | `82soBkFumyNXCL1pW2AcgopGVAWAFgMhPCoeTsoFevfN` |\n| 3 | `deposit` 1000 TUSDC | `BokdHrzCJSFvoqmWVR9yMVCkbrf5ToCfnFQcCucQBwwb` |\n| 4 | `disburse_entry<SSPX,TUSDC>` (operator lends 500 vs 100 SSPX) | `5d4y7Dt2Zybzep3uVUSZnvJDeiTC9Q98UzC7adUDpb2e` |\n| 5 | `repay` 500 → collateral back, position closed (PTB) | `3FNWn4ZxFRsaccXYGciN7qeEDRmwckVBAsqLHkShDHQm` |\n\n**Final on-chain state (pool `0x21b0d277…`):** `liquidity = 1000 TUSDC` (cash restored), `total_borrows = 0`\n(loan repaid), position object **deleted**, **100.0 SSPX collateral returned** to the borrower,\n`batch_root = 999` (the loan_commit folded into the on-chain Poseidon accumulator). `total_pending`\nstays 500 until `settle_batch` runs — correct, that step consumes the batch proof (proven separately\nin `sui move test`; the underlying `verify` primitive is proven on-chain above). The operator gate\n(`OperatorCap`) authorized the disburse; deposit/repay are wallet-signed. This is the Sui object/capability\nidiom replacing Solana PDAs + partial-sign — the first slice of the app-stack port off Solana.\n\n## Full Sui-native app stack + pentest (2026-07-14)\nThe whole platform now runs Sui-native (contract → SDK → operator API → web wallet). Contract v4\n`dregg_lending_async` @ `0x329d60b50dbaee425c6c681bab5789dbb437ddfc5ab9600ab45c75298c1d4aa4`, with a\n**non-custodial** borrow path: the borrower sends the tx + supplies collateral; the operator's dregg\nauthorization is an **ed25519 attestation over the exact loan terms, verified on-chain** via `sui::ed25519`\n(`disburse_attested`) — no OperatorCap two-party tx, no custody. `sui move test` 5/5.\n\n**Sui penetration test — 6/6 blocked** (live devnet, `app/sui-sdk/test/pentest-sui.mjs`):\n\n| Attack | Result | Why |\n|--------|--------|-----|\n| P1 forged attestation | 🛡 BLOCKED | `MoveAbort` EBadAttest — ed25519 verify fails |\n| P2 tampered terms (sign 500, submit 501) | 🛡 BLOCKED | `MoveAbort` EBadAttest — message binds every term |\n| P5 repay another's position (seize collateral) | 🛡 BLOCKED | `MoveAbort` ENotBorrower — **found + fixed in this port** (repay returned collateral to caller) |\n| P6 over-cap disburse (debt > pool cap) | 🛡 BLOCKED | `MoveAbort` EOverCap |\n| P3 junk-collateral borrow (unregistered type) | 🛡 BLOCKED | operator registry 400 |\n| P4 over-LTV borrow | 🛡 BLOCKED | server max-check + dregg 403 |\n\nP5 was a real vulnerability surfaced by building the Sui pentest — the Move `repay` returned the\n(over)collateral to whoever called it, so an attacker could pay a small debt and seize a larger\ncollateral. Fixed with a `pos.borrower == sender` check + a regression test (`repay_rejects_non_borrower`).\n\n**Verified live via the SDK + operator API on devnet:** the operator API's ed25519 attestation is\naccepted on-chain by `disburse_attested` (integration test `app/operator-api/test-borrow-sui.sh`), and the\nfull lending loop (init→deposit→disburse_attested→repay) runs through `@assay/sui-sdk`\n(`app/sui-sdk/test/onchain-attested.mjs`). The whole stack is clickable locally: `bash\napp/sui-harness/dev-up-sui.sh` → `cd app/web && npm run dev` (Sui wallet via `@mysten/dapp-kit`,\n`/faucet` for test coins).\n\n## Five full loans on the v8 stack (2026-07-14) — `app/sui-sdk/test/five-loans.mjs`\nEnd-to-end on the audited v8 contract (`0x226cb614…`) through the **hosted** operator: mint collateral\n→ `/quote` → `/borrow` (ed25519 attestation, now **type-bound** per the audit fix) → on-chain\n`disburse_attested` → repay → position closed. Across 5 crypto markets (multi-market, multi-package,\n9-decimal SOL, all 24/7 so unaffected by market hours). **5/5 completed.**\n\n| # | Market | Borrowed | Borrow tx | Repay tx | Closed |\n|---|--------|----------|-----------|----------|--------|\n| 1 | BTC | $500 | `6fomdovEZxMRA9pXAgK4TtsJFB96jLmRodaUeyhiHWAb` | `2RghKn89C2wFmKt9rUm9EuCRKHZXVyjSHNGeqLidyydw` | ✓ |\n| 2 | ETH | $500 | `8stfumYjfWZyoZGHbgscSwko5JTcXWERjMrKmuDtpYZc` | `3jd9Pgu7XXThcYqB2s2e3mzyB5tx8ATHwrWUZM1i9Xsx` | ✓ |\n| 3 | SOL | $186.31 | `B1ufZ8NCGQaQuHqNKYr2j812eKGfx8kXZZyQLYeChdgR` | `8Xux4UbUDtG3ZWQzf5B8yfKKHuU87AenXogKN4RA9P3f` | ✓ |\n| 4 | HYPE | $500 | `BctvvB5Q57Gz1dRnEFnV4cqGeMZkcAxWcCYto8XtUkni` | `GCsse5qAcYVDxammyG1nywEAW97vwsJXorAqi79PgnXJ` | ✓ |\n| 5 | LINK | $183.04 | `GmWT6pzTQgvuJc5kh3uy2WCLfCP9HxeAhFJee6s3YHSW` | `84ckt1TkRWCkTHu2gpxuAeGrRW38RVUz73JAFVxBa79M` | ✓ |\n\nExercises the full audited path (type-bound attestation, per-collateral isolation, hosted operator)\nacross markets from both coin packages, all on a public explorer (devnet).\n"}];
